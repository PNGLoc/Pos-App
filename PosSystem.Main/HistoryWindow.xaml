<Window x:Class="PosSystem.Main.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lịch sử đơn hàng hôm nay"
        Height="500"
        Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal"
                Margin="0,0,0,15">
            <TextBlock Text="📜 ĐƠN HÀNG HÔM NAY"
                    FontSize="20"
                    FontWeight="Bold"
                    Foreground="#333"/>
        </StackPanel>

        <DataGrid Grid.Row="1"
                x:Name="dgHistory"
                AutoGenerateColumns="False"
                IsReadOnly="True"
                  Background="White"
                RowHeight="45"
                CanUserAddRows="False"
                BorderThickness="1"
                BorderBrush="#EEE">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#"
                        Binding="{Binding OrderID}"
                        Width="60"/>
                <DataGridTextColumn Header="Bàn"
                        Binding="{Binding TableName}"
                        Width="120"/>

                <DataGridTextColumn Header="Giờ"
                        Binding="{Binding OrderTime, StringFormat='HH:mm'}"
                        Width="80"/>

                <DataGridTextColumn Header="Tổng tiền"
                        Binding="{Binding FinalAmount, StringFormat={}{0:N0}}"
                        Width="120"
                        FontWeight="Bold"/>

                <DataGridTemplateColumn Header="Hành động"
                        Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="👁 Chi tiết"
                                        Click="BtnView_Click"
                                        Tag="{Binding OrderID}"
                                        Margin="0,0,10,0"
                                        Padding="10,0"
                                        Height="30"
                                        Background="#6C757D"
                                        Foreground="White"
                                        BorderThickness="0"/>
                                <Button Content="🖨 In lại Bill"
                                        Click="BtnReprint_Click"
                                        Tag="{Binding OrderID}"
                                        Height="30"
                                        Padding="10,0"
                                        Background="#17A2B8"
                                        Foreground="White"
                                        BorderThickness="0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2"
                Content="Đóng"
                IsCancel="True"
                HorizontalAlignment="Right"
                Width="100"
                Height="40"
                Margin="0,15,0,0"/>
    </Grid>
</Window>